.TH _PRINTF 3 "November 2025" "Holberton Project" "Student Manual"
.SH NAME
_printf \- custom version of the C printf function
.SH SYNOPSIS
.B #include "main.h"
.PP
.BI "int _printf(const char *" format ", ...);"
.SH DESCRIPTION
The
.B _printf
function prints formatted output to the standard output.
The
.I format
string may contain ordinary characters or conversion
specifiers introduced by a
.B %
character.

This implementation supports only the following conversions:

.TP
.B %c
Print a single character.

.TP
.B %s
Print a string. If the argument is
.B NULL,
"(null)" is printed.

.TP
.B %d
.TP
.B %i
Print a signed integer in decimal format.

.TP
.B %%
Print a literal percent sign.

.SH RETURN VALUE
Returns the total number of characters printed.
If
.I format
is
.B NULL
or contains an incomplete "%", the function returns
.BR -1 .

.SH EXAMPLES
.nf
_printf("Char: %c\n", 'A');
_printf("Hello %s!\n", "world");
_printf("Value: %d\n", -42);
_printf("Percent: %%\n");
.fi


/*  
 * Authors:
 *  - Ouahes Hédy
 *  - Conzo Mwana Geoffray
 *  - El Ioussoufi Nael
 */
