.TH _PRINTF 3 "27 Nov 2025" "1.0" "holbertonschool printf man page"
.SH NAME
_printf \- produces output according to a format
.SH SYNOPSIS
#include "main.h"

int _printf(const char *format, ...);
.SH DESCRIPTION
_printf is a simplified version of the standard printf function. It writes output to
the standard output (stdout) according to the format string provided.

The function handles the following conversion specifiers:
.TP
%c
Prints a single character.
.TP
%s
Prints a string of characters. If the argument is NULL, "(null)" is printed.
.TP
%%
Prints the percent character '%'.
.TP
%d, %i
Prints a signed decimal integer.
.SH RETURN VALUE
On success, _printf returns the number of characters printed (excluding the null byte used
to end output). If an error occurs, such as a NULL format string, the function returns -1.
.SH EXAMPLES
The following examples demonstrate usage:
.nf
    _printf("Hello, World!\n");
    _printf("Character: %c\n"_

