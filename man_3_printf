.TH _PRINTF 3 "27 Nov 2025" "1.0" "holbertonschool printf man page"
.SH NAME
_printf \- Custom implementation of printf

.SH SYNOPSIS
#include "main.h"

int _printf(const char *format, ...);

.SH DESCRIPTION
_printf is a simplified version of printf handling the following specifiers:
.TP
%c
Prints a single character.
.TP
%s
Prints a string. If the argument is NULL, "(null)" is printed.
.TP
%d, %i
Prints a signed decimal integer.
.TP
%%
Prints the percent character '%'.

.SH RETURN VALUE
On success, _printf returns the number of characters printed. On error (e.g. NULL format), it returns -1.

.SH EXAMPLES
.nf
    _printf("Hello %s\n", "world");
    _printf("Number: %d\n", 42);
.fi

